# Contributing to HarmonicGalaxy

感谢你对 HarmonicGalaxy 项目的关注！我们欢迎所有形式的贡献。

## 开发环境设置

### 1. 克隆仓库

```bash
git clone https://github.com/Constellation-Intelligence-Technologies/HarmonicGalaxy.git
cd HarmonicGalaxy
```

### 2. 创建虚拟环境

推荐使用 Python 3.10+：

```bash
python3 -m venv venv
source venv/bin/activate  # Linux/macOS
# 或
venv\Scripts\activate  # Windows
```

### 3. 安装开发依赖

```bash
pip install -e ".[dev]"
```

### 4. 安装 Pre-commit Hooks

```bash
pre-commit install
```

这将确保在提交代码前自动运行代码检查和格式化。

## 开发流程

### 分支策略

- `master`: 主分支，用于生产环境
- `dev`: 开发分支，用于集成新功能
- `feature/*`: 功能分支，从 `dev` 分支创建
- `fix/*`: 修复分支，从 `dev` 或 `master` 分支创建

### 工作流程

1. **创建分支**
   ```bash
   git checkout dev
   git pull origin dev
   git checkout -b feature/your-feature-name
   ```

2. **开发代码**
   - 编写代码和测试
   - 确保代码通过所有检查

3. **运行检查**
   ```bash
   # 格式化代码
   make format
   
   # 运行 lint 检查
   make lint
   
   # 运行类型检查
   make type-check
   
   # 运行测试
   make test
   ```

4. **提交代码**
   ```bash
   git add .
   git commit -m "feat: add your feature description"
   ```
   
   提交信息应遵循 [Conventional Commits](https://www.conventionalcommits.org/) 规范：
   - `feat`: 新功能
   - `fix`: 修复 bug
   - `docs`: 文档更新
   - `style`: 代码格式调整（不影响功能）
   - `refactor`: 代码重构
   - `test`: 测试相关
   - `chore`: 构建/工具链相关

5. **推送并创建 Pull Request**
   ```bash
   git push origin feature/your-feature-name
   ```
   
   然后在 GitHub 上创建 Pull Request 到 `dev` 分支。

## 代码规范

### Python 代码风格

- 使用 **Black** 进行代码格式化（行长度 100）
- 使用 **Ruff** 进行代码检查
- 使用 **isort** 进行导入排序
- 使用 **mypy** 进行类型检查（推荐添加类型注解）

### 代码检查命令

```bash
# 自动格式化
make format

# 检查代码（不修改）
make lint

# 类型检查
make type-check
```

### 测试要求

- 所有新功能必须包含测试
- 测试覆盖率不应低于 80%
- 使用 `pytest` 作为测试框架
- 单元测试放在 `tests/unit/` 目录
- 集成测试放在 `tests/integration/` 目录

运行测试：
```bash
make test
# 或带覆盖率报告
make test-cov
```

## Pull Request 流程

1. **确保代码通过所有检查**
   - Pre-commit hooks 通过
   - 所有测试通过
   - 代码覆盖率满足要求

2. **编写清晰的 PR 描述**
   - 说明变更的目的和背景
   - 列出主要变更点
   - 如有破坏性变更，请明确标注

3. **等待代码审查**
   - 至少需要一名维护者批准
   - 根据审查意见进行修改

4. **合并到 dev 分支**
   - 审查通过后，维护者会将 PR 合并到 `dev` 分支
   - 经过充分测试后，会合并到 `master` 分支

## 项目结构

```
HarmonicGalaxy/
├── harmonicgalaxy/          # 主包
│   ├── core/                # 核心组件
│   ├── agents/              # Agent 注册和能力描述
│   ├── orchestrator/        # 编排器
│   ├── state/               # 状态管理
│   └── events/              # 事件流
├── tests/                   # 测试
│   ├── unit/                # 单元测试
│   └── integration/         # 集成测试
├── docs/                    # 文档
├── scripts/                 # 脚本工具
└── .github/                 # GitHub 配置
```

## 获取帮助

- 查看 [README.md](README.md) 了解项目概述
- 在 [Issues](https://github.com/Constellation-Intelligence-Technologies/HarmonicGalaxy/issues) 中提问或搜索
- 查看现有代码和测试示例

## 行为准则

请遵循以下原则：

- 尊重所有贡献者
- 接受建设性的批评
- 专注于对项目最有利的事情
- 对其他社区成员表示同理心

感谢你的贡献！🎉

